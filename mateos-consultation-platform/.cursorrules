# 🤖 MATEOS PLATFORM - ПРАВИЛА ДЛЯ ИИ-АССИСТЕНТА
# Версия: 1.0.0 | Дата: 2025-09-15

## 📋 ПРОЕКТ: Платформа математических консультаций Mateos

### Основная информация:
- **Стек**: React 18 + TypeScript + Vite + TailwindCSS + shadcn/ui  
- **Архитектура**: Feature-based с React Query для state management
- **Цель**: Образовательная платформа для математических консультаций
- **Статус**: В активной разработке, критическая фаза архитектурных улучшений
-- ВСЕ ДОЛЖНО БЫТЬ НА РУМЫНСКОМ ЯЗЫКЕ --
---

## 🚨 КРИТИЧЕСКИЕ ПРАВИЛА БЕЗОПАСНОСТИ

### ❌ ЗАПРЕЩЕНО БЕЗ ПОДТВЕРЖДЕНИЯ:

1. **УДАЛЕНИЕ файлов** - любых файлов, особенно:
   - `package.json`, `tsconfig.json`, конфигурационные файлы
   - Компоненты в `src/components/`  
   - Страницы в `src/pages/`
   - Сервисы в `src/services/`

2. **ПЕРЕПИСЫВАНИЕ файлов полностью**:
   - Существующие компоненты UI
   - Настроенные сервисы API
   - Contexts и Providers
   - Файлы с бизнес-логикой

3. **ИЗМЕНЕНИЕ архитектурных решений**:
   - Структуры папок в `src/`
   - Путей импорта (`@/*`)
   - Конфигураций ESLint, TypeScript
   - Зависимостей в package.json

### ⚠️ ТРЕБУЕТ СОГЛАСОВАНИЯ:

1. **Добавление новых зависимостей** в package.json
2. **Изменение существующих API интерфейсов** в `src/services/`
3. **Модификация типов** в `src/types/` и `src/entities/`
4. **Изменения в Layout, Header, Sidebar** компонентах

---

## ✅ РАЗРЕШЕННЫЕ ДЕЙСТВИЯ

### 🟢 Можно делать свободно:
- Создавать НОВЫЕ компоненты в соответствующих папках
- Добавлять утилиты в `src/lib/`
- Создавать новые хуки в `src/hooks/`
- Добавлять Stories для Storybook
- Создавать файлы документации (.md)

### 🟡 Можно с осторожностью:
- Расширять существующие компоненты (только добавление функций)
- Оптимизировать производительность
- Исправлять TypeScript ошибки
- Улучшать accessibility

---

## 🛠️ СТАНДАРТЫ РАЗРАБОТКИ

### TypeScript:
```typescript
// ✅ Всегда используй строгие типы
interface ComponentProps {
  title: string;
  onSubmit: (data: FormData) => Promise<void>;
}

// ✅ Используй exist типы из src/entities/
import { User, Consultation } from '@/entities';
```

### Компоненты:
```tsx
// ✅ Структура компонента
import { FC } from 'react';
import { cn } from '@/lib/utils';

interface Props {
  className?: string;
}

export const Component: FC<Props> = ({ className }) => {
  return (
    <div className={cn("base-classes", className)}>
      {/* content */}
    </div>
  );
};
```

### Импорты:
```typescript
// ✅ Используй алиасы
import { Button } from '@/components/ui/button';
import { useAuth } from '@/contexts/AuthContext';
import type { User } from '@/entities';
```

---

## 📁 АРХИТЕКТУРА ПРОЕКТА

### Текущая структура (НЕ МЕНЯТЬ):
```
src/
├── components/          # UI компоненты
│   ├── ui/             # shadcn/ui компоненты  
│   └── *.tsx           # Кастомные компоненты
├── contexts/           # React контексты
├── entities/           # Бизнес сущности и типы
├── features/           # Feature-based модули
├── hooks/              # Кастомные хуки
├── lib/                # Утилиты и конфигурации
├── pages/              # Страницы роутинга
├── services/           # API сервисы
├── stories/            # Storybook истории
└── types/              # Общие типы
```

### shadcn/ui интеграция:
- Компоненты устанавливать через: `npx shadcn@latest add [component]`
- Кастомизация только через className и CSS переменные
- Не модифицировать файлы в `src/components/ui/` напрямую

---

## 🔍 BEFORE CODING CHECKLIST

### Перед любым изменением:

1. **📖 ИЗУЧИТЬ КОНТЕКСТ**:
   ```bash
   # Прочитай связанные файлы:
   - README.md (обзор проекта)
   - Audit.md (технические проблемы)  
   - BACKEND_ENHANCEMENT_ROADMAP.md (планы)
   - API_INTEGRATION_GUIDE.md (API гайд)
   ```

2. **🔍 НАЙТИ СУЩЕСТВУЮЩИЕ РЕШЕНИЯ**:
   - Проверь есть ли похожий компонент
   - Изучи паттерны в текущем коде
   - Найди тесты и Stories

3. **⚠️ ПРОВЕРИТЬ ВЛИЯНИЕ**:
   - На какие компоненты повлияют изменения?
   - Сломаются ли существующие импорты?
   - Совместимо ли с API?

4. **✅ СПРОСИТЬ РАЗРЕШЕНИЯ** если:
   - Изменяешь существующие файлы
   - Добавляешь зависимости
   - Меняешь архитектуру

---

## 🎯 СПЕЦИФИКА MATEOS ПЛАТФОРМЫ

### Ключевые особенности:
- **SignalR** интеграция для real-time уведомлений
- **React Query** для управления серверным состоянием  
- **GSAP** для анимаций (оптимизировать производительность)
- **Storybook** для документации компонентов
- **TailwindCSS** + **shadcn/ui** для стилизации

### Бизнес-логика:
- Аутентификация через JWT (проблемы безопасности - см. Audit.md)
- Система консультаций с учителями и студентами  
- Платежная система (PaymentForm.tsx)
- Уведомления и чат (NotificationCenter.tsx)

### Критические области:
- ErrorBoundary требует улучшений
- Дублированные файлы (PaymentForm vs PaymentFormNew)
- AuthService безопасность
- Performance оптимизации

---

## 🔬 ТЕСТИРОВАНИЕ И КАЧЕСТВО

### Обязательно проверяй:
```bash
# TypeScript ошибки
npm run build

# Linting
npm run lint

# Storybook сборка  
npm run build-storybook
```

### Создавая компонент:
1. Добавь TypeScript интерфейсы
2. Создай Storybook историю
3. Протестируй в разных размерах экрана
4. Проверь accessibility

---

## 📞 КОММУНИКАЦИЯ С РАЗРАБОТЧИКОМ

### Всегда уведомляй о:
- Критических ошибках в коде
- Конфликтах типов TypeScript  
- Проблемах совместимости
- Необходимых архитектурных изменениях

### Спрашивай подтверждения:
- "Можно ли изменить файл X для реализации Y?"
- "Нужно ли добавить зависимость Z?"
- "Следует ли рефакторить компонент A?"

### Предлагай альтернативы:
- Вместо удаления предложи рефакторинг
- Вместо переписывания предложи расширение
- Покажи несколько вариантов решения

---

## 🎨 СТИЛЬ КОДА

### Naming conventions:
- Компоненты: `PascalCase` 
- Файлы: `camelCase.tsx`
- Функции: `camelCase`
- Constants: `UPPER_SNAKE_CASE`
- CSS classes: `kebab-case`

### Комментарии:
```typescript
// ✅ Объясняй WHY, не WHAT
const calculateOptimalTime = (session: Session) => {
  // Используем GSAP для плавной анимации переходов между консультациями
  // это критично для UX согласно исследованиям пользователей
  return gsap.timeline();
};
```

---

## 🚀 ROADMAP AWARENESS

### Текущие приоритеты (из Audit.md):
1. Исправить критические дублирования
2. Улучшить безопасность JWT  
3. Оптимизировать GSAP анимации
4. Внедрить Error Boundaries
5. Создать Design System

### Долгосрочные планы:
- Микросервисная архитектура backend
- ИИ персонализация обучения
- Геймификация платформы
- Мобильные приложения

**При разработке учитывай эти планы для совместимости!**

---

## 📋 CHECKLIST ДЛЯ КАЖДОЙ ЗАДАЧИ

- [ ] Изучил текущий код и документацию
- [ ] Понял бизнес-требования  
- [ ] Проверил совместимость с архитектурой
- [ ] Спросил разрешение на изменения (если нужно)
- [ ] Написал типизированный код
- [ ] Протестировал функциональность
- [ ] Обновил документацию (если нужно)
- [ ] Создал/обновил Stories (если нужно)

---

**🎯 ПОМНИ: Цель - сделать платформу лучше, сохранив стабильность и архитектурную целостность!**